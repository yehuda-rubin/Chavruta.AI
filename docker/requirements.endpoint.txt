# Endpoint image: the RAG query service.
# torch is NOT listed here on purpose — the Dockerfile installs it first from the CPU wheel index,
# because the default PyPI wheel bundles the CUDA runtime (~2.5GB) this CPU-only image cannot use.
# Pins are the versions the suite and a live grounded query were actually verified against
# (2026-07-17), not guesses. Keep in sync with requirements.txt so dev and the image agree.
fastapi==0.137.1
uvicorn[standard]==0.48.0
pydantic==2.13.4
FlagEmbedding==1.4.0
qdrant-client==1.18.0
openai==2.41.1
numpy==2.4.6
sentence-transformers==5.5.1
PyJWT[crypto]==2.13.0
sentry-sdk[fastapi]==2.66.1
