# ===========================
# Chavruta.AI — Dependencies
# ===========================

# Embeddings (bge-m3, multilingual HE+EN — dense + learned sparse for hybrid retrieval)
FlagEmbedding>=1.2.10
sentence-transformers>=3.0.0
numpy>=1.26

# Vector store — Qdrant (embedded local / server)
qdrant-client>=1.9

# Corpus + dataset hosting (Sefaria API, Hugging Face Hub)
requests==2.32.3
huggingface_hub>=0.23
tqdm==4.66.4

# Generation via the Nebius API (OpenAI-compatible Token Factory). The local Ollama/DictaLM backend
# was removed; the only other backend (bridge) needs no extra dependency.
openai>=1.30

# API — the served product (app/api.py). Without these `uvicorn app.api:app` fails to import.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# Dev / config
python-dotenv==1.0.1
pytest==8.2.2
ruff>=0.5.0
