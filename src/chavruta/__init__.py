"""Chavruta.AI — a trustworthy Jewish-text study partner (RAG over the Jewish bookshelf).

Deployment-agnostic core: the same code runs fully offline on a personal machine and as a
scalable cloud product, switched by configuration only (Constitution Principle II).

Knowledge lives in the retrieval index, never in the model weights (Principle I).
"""

__version__ = "0.2.0"
