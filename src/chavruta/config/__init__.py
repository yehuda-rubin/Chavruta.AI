"""Deployment configuration — the `Profile` object and the collection every backend reads."""

from chavruta.config.profile import DEFAULT_COLLECTION, Profile

__all__ = ["DEFAULT_COLLECTION", "Profile"]
